{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>{{ 'show.title.show_category'|trans({}, 'SandboxAssortmentBundleCategory') }}<small> {{ 'show.title.show_category_description'|trans({}, 'SandboxAssortmentBundleCategory') }}</small></h1>
</div>

<div class="well">
<a href="{{ path('sylius_assortment_backend_product_create') }}" class="btn btn-primary">
    <i class="icon-plus icon-white"></i> {{ 'show.button.create_product'|trans({}, 'SandboxAssortmentBundleCategory') }}
</a>
</div>

{% if paginator.haveToPaginate() %}
{{ pagerfanta(paginator, 'twitter_bootstrap', {'prev_message': "previous page...", 'next_message': "next page..."}) }}
{% endif %}

{% if products|length > 0 %}
<table class="table">
    <thead>
        <tr>
            <th><a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}">{{ 'show.table.product_id'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
            <th><a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}">{{ 'show.table.product_name'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
            <th><a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}">{{ 'show.table.product_price'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
            <th><a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}">{{ 'show.table.product_created_at'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for product in products %}
        <tr>
            <td>{{ product.id }}</td>
            <td>{{ product.name }}</td>
            <td>{{ product.price }} &euro;</td>
            <td>{{ product.createdAt|date('Y-m-d H:i:s') }}</td>
            <td>
                <div class="btn-group pull-right">
                    <a href="{{ path('sylius_assortment_backend_product_show', {'id': product.id}) }}" class="btn">
                       <i class="icon-book"></i> {{ 'show.button.product_show'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
                    </a>
                    <a href="{{ path('sylius_assortment_backend_product_update', {'id': product.id}) }}" class="btn">
                       <i class="icon-pencil"></i> {{ 'show.button.product_update'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
                    </a>
                    <a href="{{ path('sylius_assortment_backend_product_delete', {'id': product.id}) }}" class="btn btn-danger confirmer"
                        data-confirmer-question="{{ 'show.dialog.product_confirm_delete'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>"
                    >
                        <i class="icon-remove icon-white"></i> {{ 'show.button.product_delete'|trans({}, 'SandboxAssortmentBundleCategory') }}</a></th>
                    </a>
                </div>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% else %}
<div class="alert alert-info">
    <h4 class="alert-heading">{{ 'show.information.title'|trans({}, 'SandboxAssortmentBundleCategory') }}</h4>
    {{ 'show.information.no_products'|trans({}, 'SandboxAssortmentBundleCategory') }}
</div>
{% endif %}

{% if paginator.haveToPaginate() %}
{{ pagerfanta(paginator, 'twitter_bootstrap', {'prev_message': "previous page...", 'next_message': "next page..."}) }}
{% endif %}

{% endblock %}
