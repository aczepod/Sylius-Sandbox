{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>{{ 'update.title.update_category'|trans({}, 'SandboxBloggerBundleCategory') }}<small> {{ 'update.title.update_category_description'|trans({}, 'SandboxBloggerBundleCategory') }}</small></h1>
</div>

<form action="{{ path('sylius_categorizer_backend_category_update', {'alias': catalog.alias, 'id': category.id}) }}" method="post" class="form-horizontal">
<fieldset>
    {{ form_row(form.name, {'attr': {'class': 'span7'}}) }}
</fieldset>
    {{ form_rest(form) }}
    <div class="form-actions">
    <input type="submit" value="{{ 'update.button.save_changes'|trans({}, 'SandboxBloggerBundleCategory') }}" class="btn btn-primary btn-large" />
    </div>
</form>
{% endblock %}
