{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block content %}

<div class="page-header">
    <h1>{{ 'category.list.title.list_categories'|trans({}, 'SandboxAssortmentBundle') }}<small> {{ 'category.list.title.list_categories_description'|trans({}, 'SandboxAssortmentBundle') }}</small></h1>
</div>

<div class="well">
<a href="{{ path('sylius_categorizer_backend_category_create', {'alias': catalog.alias}) }}" class="btn btn-primary">
    <i class="icon-plus icon-white"></i> {{ 'category.list.button.create_category'|trans({}, 'SandboxAssortmentBundle') }}
</a>
</div>

{% if categories %}
<table class="table">
    <thead>
        <tr>
            <th>{{ 'category.list.table.category_id'|trans({}, 'SandboxAssortmentBundle') }}</th>
            <th>{{ 'category.list.table.category_name'|trans({}, 'SandboxAssortmentBundle') }}</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <div class="pull-right btn-toolbar">
                        <div class="btn-group">
                            {% if loop.first %}
                            <a class="btn disabled">&uarr;</a>
                            {% else %}
                            <a href="{{ path('sylius_categorizer_backend_category_move_up', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">&uarr;</a>
                            {% endif %}

                            {% if loop.last %}
                            <a class="btn disabled">&darr;</a>
                            {% else %}
                            <a href="{{ path('sylius_categorizer_backend_category_move_down', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">&darr;</a>
                            {% endif %}
                        </div>
                        <div class="btn-group">
                            <a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">
                                <i class="icon-list-alt"></i> {{ 'category.list.button.list_products'|trans({}, 'SandboxAssortmentBundle') }}
                            </a>
                            <a href="{{ path('sylius_categorizer_backend_category_update', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">
                                <i class="icon-pencil"></i> {{ 'category.list.button.update'|trans({}, 'SandboxAssortmentBundle') }}
                            </a>
                            <a href="{{ path('sylius_categorizer_backend_category_delete', {'alias': catalog.alias, 'id': category.id}) }}" class="btn btn-danger confirmer"
                                data-confirmer-question="{{ 'category.list.dialog.category_confirm_delete'|trans({}, 'SandboxAssortmentBundle') }}"
                            >
                                <i class="icon-remove icon-white"></i> {{ 'category.list.button.delete'|trans({}, 'SandboxAssortmentBundle') }}
                            </a>
                        </div>
                    </div>
                </td>
            </tr>

            {% if category['__children'] is defined %}
                {% include 'SandboxAssortmentBundle:Backend/Category:node.html.twig' %}
            {% endif %}
            {% endfor %}
    </tbody>
</table>
{% else %}
<div class="alert alert-info">
    <h4 class="alert-heading">{{ 'category.list.information.title'|trans({}, 'SandboxAssortmentBundle') }}</h4>
    There are no categories to display.
    {{ 'category.list.information.no_categories'|trans({}, 'SandboxAssortmentBundle') }}
</div>
{% endif %}

{% endblock %}
