{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>{{ 'list.title.list_categories'|trans({}, 'SandboxBloggerBundleCategory') }}<small> {{ 'list.title.list_categories_description'|trans({}, 'SandboxBloggerBundleCategory') }}</small></h1>
{% endblock %}

{% block content %}
<div class="well">
    <a href="{{ path('sylius_categorizer_backend_category_create', {'alias': catalog.alias}) }}" class="btn btn-primary">
        <i class="icon-plus icon-white"></i> {{ 'list.button.create_category'|trans({}, 'SandboxBloggerBundleCategory') }}
    </a>
</div>

{% if categories %}
<table class="table">
    <thead>
        <tr>
            <th>{{ 'list.table.category_id'|trans({}, 'SandboxBloggerBundleCategory') }}</th>
            <th>{{ 'list.table.category_name'|trans({}, 'SandboxBloggerBundleCategory') }}</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <div class="pull-right btn-toolbar">
                        <div class="btn-group">
                            {% if loop.first %}
                            <a class="btn disabled">&uarr;</a>
                            {% else %}
                            <a href="{{ path('sylius_categorizer_backend_category_move_up', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">&uarr;</a>
                            {% endif %}

                            {% if loop.last %}
                            <a class="btn disabled">&darr;</a>
                            {% else %}
                            <a href="{{ path('sylius_categorizer_backend_category_move_down', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">&darr;</a>
                            {% endif %}
                        </div>
                        <div class="btn-group">
                            <a href="{{ path('sylius_categorizer_backend_category_show', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">
                                <i class="icon-list-alt"></i> {{ 'list.button.list_posts'|trans({}, 'SandboxBloggerBundleCategory') }}
                            </a>
                            <a href="{{ path('sylius_categorizer_backend_category_update', {'alias': catalog.alias, 'id': category.id}) }}" class="btn">
                                <i class="icon-pencil"></i> {{ 'list.button.update'|trans({}, 'SandboxBloggerBundleCategory') }}
                            </a>
                            <a href="{{ path('sylius_categorizer_backend_category_delete', {'alias': catalog.alias, 'id': category.id}) }}" class="btn btn-danger confirmer"
                                data-confirmer-question="{{ 'list.dialog.category_confirm_delete'|trans({}, 'SandboxBloggerBundleCategory') }}"
                            >
                                <i class="icon-remove icon-white"></i> {{ 'list.button.delete'|trans({}, 'SandboxBloggerBundleCategory') }}
                            </a>
                        </div>
                    </div>
                </td>
            </tr>
            {% endfor %}
    </tbody>
</table>
{% else %}
<div class="alert alert-info">
    <h4 class="alert-heading">{{ 'list.information.title'|trans({}, 'SandboxBloggerBundleCategory') }}</h4>
    {{ 'list.information.no_categories'|trans({}, 'SandboxBloggerBundleCategory') }}
</div>
{% endif %}
{% endblock %}
