{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>{{ 'update.title.update_category'|trans({}, 'SandboxAssortmentBundleCategory') }}<small> {{ 'update.title.update_category_description'|trans({}, 'SandboxAssortmentBundleCategory') }}</small></h1>
</div>

<form action="{{ path('sylius_categorizer_backend_category_update', {'alias': catalog.alias, 'id': category.id}) }}" method="post" class="form-horizontal">
<fieldset>
    {{ form_row(form.name, {'attr': {'class': 'span7'}}) }}
    {{ form_row(form.parent, {'empty_value': 'update.select.choose_category'|trans({}, 'SandboxAssortmentBundleCategory')}) }}
</fieldset>
    {{ form_rest(form) }}
    <div class="form-actions">
    <input type="submit" value="{{ 'update.button.save_changes'|trans({}, 'SandboxAssortmentBundleCategory') }}" class="btn btn-primary btn-large" />
    </div>
</form>
{% endblock %}
